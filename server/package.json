{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "json-server --delay 500 --port 3004 --watch db.json "
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {},
  "dependencies": {
    "json-server": "^0.16.3"
  }
}
